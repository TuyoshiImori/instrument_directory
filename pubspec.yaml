name: instrument_directory
description: A new Flutter application.

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  provider: ^5.0.0
  app_review: ^2.0.1
  flutter_vector_icons: ^1.0.0
  flutter_email_sender: ^5.0.0
  url_launcher: ^6.0.2 #要注意
  flutter_datetime_picker: ^1.5.0
  path_provider: ^2.0.1
  image_picker: ^0.7.3
  image_cropper: ^1.4.0

  flutter_sticky_header: ^0.5.0


  google_fonts: ^2.0.0
  shared_preferences: ^2.0.5
  font_awesome_flutter: ^9.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true
  assets:
    - assets/